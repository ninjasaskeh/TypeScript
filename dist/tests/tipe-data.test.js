"use strict";
describe('Data Type', () => {
    it('should must declare', () => {
        let name = "Arief Maizaki";
        let balance = 100000;
        let isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
        // name = 1;
        // balance = "100";
        // isVip = 1;
    });
});
