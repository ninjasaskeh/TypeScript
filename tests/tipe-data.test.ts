describe('Data Type', () => {
    it('should must declare', () => {
        let name: string = "Arief Maizaki"
        let balance: number = 100000
        let isVip: boolean = true

        console.info(name)
        console.info(balance)
        console.info(isVip)

        // name = 1;
        // balance = "100";
        // isVip = 1;

    });
});